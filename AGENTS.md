# AGENTS.md

STOG 저장소에서 작업하는 에이전트를 위한 진입점입니다.

## 가장 중요한 규칙

**이 파일에서 추론하지 마세요.** 사실은 전부 `docs/*.yaml`에 있습니다. 이 파일은 어디를 읽어야 하는지만 알려줍니다.

문서에 없는 내용을 만나면 **추측해서 채우지 말고** 작업을 멈추고 물어보세요. 이 프로젝트에서 가장 비싼 실수는 코드 오류가 아니라 **각자 다른 가정으로 만든 것이 나중에 합쳐지지 않는 것**입니다.

## 읽는 순서

| 순서 | 파일 | 언제 |
|---|---|---|
| 1 | `docs/glossary.yaml` | **항상 먼저.** 용어와 금지 표현 |
| 2 | `docs/product.yaml` | 무엇을 왜 만드는지, 기능 ID, 결정 이력 |
| 3 | `docs/architecture.yaml` | 스택, DB 스키마, API, 알고리즘 |
| 4 | `docs/conventions.yaml` | Git 규칙, 방법론 범위 |
| 5 | `docs/pitfalls.yaml` | 자기 역할 섹션 |

`OVERVIEW.md`는 사람이 읽는 문서입니다. 충돌하면 **yaml이 우선합니다.**

## 참고 가이드

외부 앱 공유 담기와 여행 가이드 AI에 대한 조사·제품 피드백 요약은
[`docs/stog-share-import-guide.md`](docs/stog-share-import-guide.md)에
있습니다. 이 문서는 참고 자료이며 확정된 사실이나 기능 범위가 아닙니다.
구현 시에는 반드시 위 YAML 문서를 기준으로 삼습니다.

## 코드를 쓰기 전에

**용어를 확인하세요.** `glossary.yaml`의 `en` 값을 변수명과 테이블명에 그대로 씁니다. `avoid` 목록의 표현은 코드·주석·커밋 메시지 어디에도 쓰지 않습니다.

자주 틀리는 것들입니다.

- 셀은 `cell`입니다. `block`, `hexagon`, `grid`가 아닙니다
- 사진을 찍은 좌표는 `spot`, 검색되는 상호는 `place`입니다. 다른 개념입니다
- 여행 동선은 `trail`입니다. `route`, `path`가 아닙니다

**결정 이력을 확인하세요.** `product.yaml`의 `decisions`에는 `rejected` 필드가 붙어 있습니다. "이거 이렇게 하면 좋겠는데"라고 생각되는 것 대부분은 이미 검토하고 뺀 것입니다.

**숫자를 박지 마세요.** 30분, 50m, 0.3 같은 값은 `product.yaml`의 `tuning_parameters`에서 읽습니다. 코드에 상수로 넣지 않습니다.

## 절대 하지 않는 것

`conventions.yaml`의 `over_engineering.forbidden`에 전체 목록이 있습니다. 자주 나오는 것만 옮기면 이렇습니다.

- `cells` 테이블 생성 — 셀은 좌표로 계산됩니다
- 서버 경유 사진 업로드 — 서명 URL로 앱이 직접 올립니다
- PostGIS, WebSocket, 마이크로서비스, 벡터 DB, 학습 기반 추천 모델
- APK에 API 시크릿 포함

## 스펙과 코드

**yaml이 단일 진실입니다.** 코드가 문서와 다르면 코드가 틀린 것입니다.

동작이 달라지는 변경이라면 **같은 PR에 yaml 변경을 포함**하세요. 코드만 바꾸고 문서를 나중에 맞추는 순서는 금지입니다.

## 브랜치와 PR

```
feat/F-06-card-dashboard
fix/F-13-coupon-rollback
```

`F-xx`는 `product.yaml`의 기능 ID와 1:1로 대응합니다.

PR에는 세 가지가 필요합니다.

1. 기능 ID
2. 스펙 변경 여부 (있으면 어느 yaml의 어느 항목인지)
3. **영향받는 역할 체크박스** — FE / BE / DB / AI

3번을 비워두고 머지한 뒤 문제가 생기면 몰랐던 게 아니라 알리지 않은 것입니다.

## 테스트

전 영역을 테스트하지 않습니다. **판정 로직만** 씁니다.

- 방문/통과 판정
- 쿠폰 발급 조건과 롤백
- 성향 점수 산출
- 좌표에서 셀 변환, 셀 전환 확인
- 동선 보간 속도 판정

전부 순수 함수라 테스트가 쉽고 틀리면 치명적입니다. UI·통합·E2E는 하지 않습니다.

## 문서에 없는 상황이 생기면

1. **임의로 결정하지 않습니다**
2. 해당 yaml의 `open_items`에 질문을 추가합니다
3. 채널에 알리고 담당 역할의 답을 받습니다
4. 결정되면 `decisions`로 옮기고 `CHANGELOG.yaml`에 기록합니다

기다리면 작업이 멈추는 경우에만, **가장 되돌리기 쉬운 선택**을 하고 PR 설명에 임시 결정임을 명시한 뒤 다음 동기화에서 안건으로 올립니다.
